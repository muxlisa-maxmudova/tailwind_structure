const UserContact = ({result}:{result}) => {
    return (
        <div>
            <ul role="list" className="divide-y divide-gray-100">
                {result}
            </ul>
        </div>
    )
}
export default UserContact
